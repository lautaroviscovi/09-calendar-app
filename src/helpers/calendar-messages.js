/**
 * Modifico 'next' y 'prev' por < >
 * para una mejor vista.
 * Añado el 'showMore' para ver
 * todos los eventos cuando se montan
 * uno encima de otro
 */
export const messages = {
    previous: '<',
    next: '>',
    showMore: total => `+ Show more (${total})`
};